---
# Override these in inventory or group_vars. Do not hardcode secrets here.
admin_user: deploy
allowed_ssh_cidrs:
  - "0.0.0.0/0"   # REPLACE with the client's specified admin IP(s), e.g. 203.0.113.4/32
ssh_port: 22
timezone: "Europe/Dublin"
